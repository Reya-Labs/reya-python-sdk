# coding: utf-8

"""
    Reya DEX Trading API v2

    No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

    The version of the OpenAPI document: 3.0.7
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
from enum import Enum
from typing_extensions import Self


class CancelReason(str, Enum):
    """
    Why an order reached a terminal `CANCELLED` status. Present only when `status` is `CANCELLED`. `NO_LIQUIDITY` (IOC found no resting liquidity) and `IOC_REMAINDER` (IOC partially filled and the remaining quantity was cancelled because no more executable opposite liquidity was available) are returned on `createOrder` / `modifyOrder` responses; `GTT_EXPIRED`, `USER_CANCEL`, `MASS_CANCEL` and `CANCEL_ALL_AFTER` are delivered on the `walletOrderChanges` stream. `SELF_TRADE_PREVENTION` (the order would have matched your own resting order) cancels the **aggressor (taker) only** - your resting order is left untouched, so there is no resting-side STP event. It appears on the response when a self-crossing IOC or modify cancels the taker, and on the `walletOrderChanges` stream when a self-crossing GTC aggressor is cancelled. If an IOC partially fills and its remaining quantity is then cancelled by self-trade prevention, the response keeps the fill quantities and reports `cancelReason: SELF_TRADE_PREVENTION`; `IOC_REMAINDER` is used only when the remaining quantity is cancelled because matching stops without a self-cross.
    """

    """
    allowed enum values
    """
    NO_LIQUIDITY = 'NO_LIQUIDITY'
    IOC_REMAINDER = 'IOC_REMAINDER'
    SELF_TRADE_PREVENTION = 'SELF_TRADE_PREVENTION'
    GTT_EXPIRED = 'GTT_EXPIRED'
    USER_CANCEL = 'USER_CANCEL'
    MASS_CANCEL = 'MASS_CANCEL'
    CANCEL_ALL_AFTER = 'CANCEL_ALL_AFTER'

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Create an instance of CancelReason from a JSON string"""
        return cls(json.loads(json_str))



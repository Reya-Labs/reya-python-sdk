# coding: utf-8

"""
    Reya DEX Trading API v2

    No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

    The version of the OpenAPI document: 3.3.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501

import warnings
from pydantic import validate_call, Field, StrictFloat, StrictStr, StrictInt
from typing import Any, Dict, List, Optional, Tuple, Union
from typing_extensions import Annotated

from sdk.open_api.models.cancel_all_after_request import CancelAllAfterRequest
from sdk.open_api.models.cancel_all_after_response import CancelAllAfterResponse
from sdk.open_api.models.cancel_order_request import CancelOrderRequest
from sdk.open_api.models.cancel_order_response import CancelOrderResponse
from sdk.open_api.models.create_order_request import CreateOrderRequest
from sdk.open_api.models.create_order_response import CreateOrderResponse
from sdk.open_api.models.mass_cancel_request import MassCancelRequest
from sdk.open_api.models.mass_cancel_response import MassCancelResponse
from sdk.open_api.models.modify_order_request import ModifyOrderRequest
from sdk.open_api.models.modify_order_response import ModifyOrderResponse

from sdk.open_api.api_client import ApiClient, RequestSerialized
from sdk.open_api.api_response import ApiResponse
from sdk.open_api.rest import RESTResponseType


class OrderEntryApi:
    """NOTE: This class is auto generated by OpenAPI Generator
    Ref: https://openapi-generator.tech

    Do not edit the class manually.
    """

    def __init__(self, api_client=None) -> None:
        if api_client is None:
            api_client = ApiClient.get_default()
        self.api_client = api_client


    @validate_call
    async def cancel_all(
        self,
        mass_cancel_request: MassCancelRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> MassCancelResponse:
        """Cancel all orders

        Cancel all orders matching the specified filters (mass cancel). Supports both spot and perp markets. Also available on the order-entry WebSocket (`asyncapi-exec-v2.yaml`, `cancelAll`).

        :param mass_cancel_request: (required)
        :type mass_cancel_request: MassCancelRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._cancel_all_serialize(
            mass_cancel_request=mass_cancel_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "MassCancelResponse",
            '400': "RequestError",
            '500': "ServerError",
        }
        response_data = await self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        await response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    async def cancel_all_with_http_info(
        self,
        mass_cancel_request: MassCancelRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[MassCancelResponse]:
        """Cancel all orders

        Cancel all orders matching the specified filters (mass cancel). Supports both spot and perp markets. Also available on the order-entry WebSocket (`asyncapi-exec-v2.yaml`, `cancelAll`).

        :param mass_cancel_request: (required)
        :type mass_cancel_request: MassCancelRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._cancel_all_serialize(
            mass_cancel_request=mass_cancel_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "MassCancelResponse",
            '400': "RequestError",
            '500': "ServerError",
        }
        response_data = await self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        await response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    async def cancel_all_without_preload_content(
        self,
        mass_cancel_request: MassCancelRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Cancel all orders

        Cancel all orders matching the specified filters (mass cancel). Supports both spot and perp markets. Also available on the order-entry WebSocket (`asyncapi-exec-v2.yaml`, `cancelAll`).

        :param mass_cancel_request: (required)
        :type mass_cancel_request: MassCancelRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._cancel_all_serialize(
            mass_cancel_request=mass_cancel_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "MassCancelResponse",
            '400': "RequestError",
            '500': "ServerError",
        }
        response_data = await self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _cancel_all_serialize(
        self,
        mass_cancel_request,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        # process the query parameters
        # process the header parameters
        # process the form parameters
        # process the body parameter
        if mass_cancel_request is not None:
            _body_params = mass_cancel_request


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json'
                ]
            )

        # set the HTTP header `Content-Type`
        if _content_type:
            _header_params['Content-Type'] = _content_type
        else:
            _default_content_type = (
                self.api_client.select_header_content_type(
                    [
                        'application/json'
                    ]
                )
            )
            if _default_content_type is not None:
                _header_params['Content-Type'] = _default_content_type

        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='POST',
            resource_path='/cancelAll',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    async def cancel_all_after(
        self,
        cancel_all_after_request: CancelAllAfterRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> CancelAllAfterResponse:
        """Set cancel-all-after countdown

        Arm, refresh, or disarm an account-scoped dead-man's-switch countdown. While armed, all open orders for `accountId` — the same scope as `POST /v2/cancelAll` with no `symbol` filter, EXCEPT protective stops (`STOP_LOSS` / `TAKE_PROFIT`), which are deliberately left in place whether still armed in the matching engine's trigger store or already fired into a resting child — are mass-cancelled if the countdown is not refreshed with another call before `timeoutMs` elapses. `timeoutMs: 0` disarms; non-zero values must be within [5000, 60000] milliseconds. Refresh is explicit only — order entry, WebSocket ping/pong, and connection close neither refresh nor trigger the countdown. `accountId`, `timeoutMs`, `nonce`, and `deadline` are bound into the EIP-712 `CancelAllAfter` signature; `deadline` is the signature-validity window (Unix seconds), not the countdown trigger time. The response returns an advisory trigger timestamp (`triggerAt`, milliseconds; omitted when disarming). Also available on the order-entry WebSocket (`asyncapi-exec-v2.yaml`, `cancelAllAfter`). 

        :param cancel_all_after_request: (required)
        :type cancel_all_after_request: CancelAllAfterRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._cancel_all_after_serialize(
            cancel_all_after_request=cancel_all_after_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "CancelAllAfterResponse",
            '400': "RequestError",
            '500': "ServerError",
        }
        response_data = await self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        await response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    async def cancel_all_after_with_http_info(
        self,
        cancel_all_after_request: CancelAllAfterRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[CancelAllAfterResponse]:
        """Set cancel-all-after countdown

        Arm, refresh, or disarm an account-scoped dead-man's-switch countdown. While armed, all open orders for `accountId` — the same scope as `POST /v2/cancelAll` with no `symbol` filter, EXCEPT protective stops (`STOP_LOSS` / `TAKE_PROFIT`), which are deliberately left in place whether still armed in the matching engine's trigger store or already fired into a resting child — are mass-cancelled if the countdown is not refreshed with another call before `timeoutMs` elapses. `timeoutMs: 0` disarms; non-zero values must be within [5000, 60000] milliseconds. Refresh is explicit only — order entry, WebSocket ping/pong, and connection close neither refresh nor trigger the countdown. `accountId`, `timeoutMs`, `nonce`, and `deadline` are bound into the EIP-712 `CancelAllAfter` signature; `deadline` is the signature-validity window (Unix seconds), not the countdown trigger time. The response returns an advisory trigger timestamp (`triggerAt`, milliseconds; omitted when disarming). Also available on the order-entry WebSocket (`asyncapi-exec-v2.yaml`, `cancelAllAfter`). 

        :param cancel_all_after_request: (required)
        :type cancel_all_after_request: CancelAllAfterRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._cancel_all_after_serialize(
            cancel_all_after_request=cancel_all_after_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "CancelAllAfterResponse",
            '400': "RequestError",
            '500': "ServerError",
        }
        response_data = await self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        await response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    async def cancel_all_after_without_preload_content(
        self,
        cancel_all_after_request: CancelAllAfterRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Set cancel-all-after countdown

        Arm, refresh, or disarm an account-scoped dead-man's-switch countdown. While armed, all open orders for `accountId` — the same scope as `POST /v2/cancelAll` with no `symbol` filter, EXCEPT protective stops (`STOP_LOSS` / `TAKE_PROFIT`), which are deliberately left in place whether still armed in the matching engine's trigger store or already fired into a resting child — are mass-cancelled if the countdown is not refreshed with another call before `timeoutMs` elapses. `timeoutMs: 0` disarms; non-zero values must be within [5000, 60000] milliseconds. Refresh is explicit only — order entry, WebSocket ping/pong, and connection close neither refresh nor trigger the countdown. `accountId`, `timeoutMs`, `nonce`, and `deadline` are bound into the EIP-712 `CancelAllAfter` signature; `deadline` is the signature-validity window (Unix seconds), not the countdown trigger time. The response returns an advisory trigger timestamp (`triggerAt`, milliseconds; omitted when disarming). Also available on the order-entry WebSocket (`asyncapi-exec-v2.yaml`, `cancelAllAfter`). 

        :param cancel_all_after_request: (required)
        :type cancel_all_after_request: CancelAllAfterRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._cancel_all_after_serialize(
            cancel_all_after_request=cancel_all_after_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "CancelAllAfterResponse",
            '400': "RequestError",
            '500': "ServerError",
        }
        response_data = await self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _cancel_all_after_serialize(
        self,
        cancel_all_after_request,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        # process the query parameters
        # process the header parameters
        # process the form parameters
        # process the body parameter
        if cancel_all_after_request is not None:
            _body_params = cancel_all_after_request


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json'
                ]
            )

        # set the HTTP header `Content-Type`
        if _content_type:
            _header_params['Content-Type'] = _content_type
        else:
            _default_content_type = (
                self.api_client.select_header_content_type(
                    [
                        'application/json'
                    ]
                )
            )
            if _default_content_type is not None:
                _header_params['Content-Type'] = _default_content_type

        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='POST',
            resource_path='/cancelAllAfter',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    async def cancel_order(
        self,
        cancel_order_request: CancelOrderRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> CancelOrderResponse:
        """Cancel order

        Cancel an existing order. Supports both spot and perp markets. All single cancels — spot and perp, including TP/SL — route through the matching engine on a unified `marketId` namespace. `accountId`, `nonce`, and `deadline` are required and bound into the EIP-712 signature.  For a `STOP_LOSS` / `TAKE_PROFIT`, cancellation is bound to the **arming signer**: the wallet that signs this cancel must be the exact wallet that armed the trigger, so a different signer holding trade permission on the same account cannot cancel it and is rejected with `UNAUTHORIZED_ACCOUNT_ERROR`. The same binding applies to `modifyOrder` on an armed trigger.  Also available on the order-entry WebSocket (`asyncapi-exec-v2.yaml`, `cancelOrder`). 

        :param cancel_order_request: (required)
        :type cancel_order_request: CancelOrderRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._cancel_order_serialize(
            cancel_order_request=cancel_order_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "CancelOrderResponse",
            '400': "RequestError",
            '500': "ServerError",
        }
        response_data = await self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        await response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    async def cancel_order_with_http_info(
        self,
        cancel_order_request: CancelOrderRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[CancelOrderResponse]:
        """Cancel order

        Cancel an existing order. Supports both spot and perp markets. All single cancels — spot and perp, including TP/SL — route through the matching engine on a unified `marketId` namespace. `accountId`, `nonce`, and `deadline` are required and bound into the EIP-712 signature.  For a `STOP_LOSS` / `TAKE_PROFIT`, cancellation is bound to the **arming signer**: the wallet that signs this cancel must be the exact wallet that armed the trigger, so a different signer holding trade permission on the same account cannot cancel it and is rejected with `UNAUTHORIZED_ACCOUNT_ERROR`. The same binding applies to `modifyOrder` on an armed trigger.  Also available on the order-entry WebSocket (`asyncapi-exec-v2.yaml`, `cancelOrder`). 

        :param cancel_order_request: (required)
        :type cancel_order_request: CancelOrderRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._cancel_order_serialize(
            cancel_order_request=cancel_order_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "CancelOrderResponse",
            '400': "RequestError",
            '500': "ServerError",
        }
        response_data = await self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        await response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    async def cancel_order_without_preload_content(
        self,
        cancel_order_request: CancelOrderRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Cancel order

        Cancel an existing order. Supports both spot and perp markets. All single cancels — spot and perp, including TP/SL — route through the matching engine on a unified `marketId` namespace. `accountId`, `nonce`, and `deadline` are required and bound into the EIP-712 signature.  For a `STOP_LOSS` / `TAKE_PROFIT`, cancellation is bound to the **arming signer**: the wallet that signs this cancel must be the exact wallet that armed the trigger, so a different signer holding trade permission on the same account cannot cancel it and is rejected with `UNAUTHORIZED_ACCOUNT_ERROR`. The same binding applies to `modifyOrder` on an armed trigger.  Also available on the order-entry WebSocket (`asyncapi-exec-v2.yaml`, `cancelOrder`). 

        :param cancel_order_request: (required)
        :type cancel_order_request: CancelOrderRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._cancel_order_serialize(
            cancel_order_request=cancel_order_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "CancelOrderResponse",
            '400': "RequestError",
            '500': "ServerError",
        }
        response_data = await self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _cancel_order_serialize(
        self,
        cancel_order_request,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        # process the query parameters
        # process the header parameters
        # process the form parameters
        # process the body parameter
        if cancel_order_request is not None:
            _body_params = cancel_order_request


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json'
                ]
            )

        # set the HTTP header `Content-Type`
        if _content_type:
            _header_params['Content-Type'] = _content_type
        else:
            _default_content_type = (
                self.api_client.select_header_content_type(
                    [
                        'application/json'
                    ]
                )
            )
            if _default_content_type is not None:
                _header_params['Content-Type'] = _default_content_type

        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='POST',
            resource_path='/cancelOrder',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    async def create_order(
        self,
        create_order_request: CreateOrderRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> CreateOrderResponse:
        """Create order

        Create a new order (IOC, GTC, GTT, SL, TP). Supports both spot and perp markets.  All order types route through the matching engine on a unified `marketId` namespace; the legacy off-chain conditional-order (DB-backed) path is retired. `timeInForce` is REQUIRED on every create, for every order class — earlier revisions let a trigger create omit it, implying `GTC`; from 3.1.0 an omitted `timeInForce` fails schema validation. Triggers are **perp-only** — a `STOP_LOSS` / `TAKE_PROFIT` on a spot market is rejected.  **Armed phase.** A `STOP_LOSS` / `TAKE_PROFIT` create carries `orderType` + `triggerPx` end-to-end and is **armed and tracked** by the matching engine: the trigger is held in the trigger store rather than the book, so it is not executable liquidity while armed. It stays modifiable and cancellable, and surfaces as `OPEN` on `openOrders`, the `walletOrderChanges` stream, and order history — carrying `triggered: false` on the first two. Order-history rows omit `triggered` entirely: the stored history has no armed-vs-fired discriminator, so its absence there means unknown rather than `false`.  **Choosing the fired child.** `timeInForce` on a trigger create chooses what the stop becomes when it fires: `IOC` (fill what the book offers, cancel the remainder), `GTC` (rest the remainder until cancelled) or `GTT` (rest it until the shared deadline below). A `GTT` trigger MUST carry a future `expiresAfter`; `GTC` and `IOC` triggers MUST omit it — an `IOC` trigger that carries one is rejected with `INPUT_VALIDATION_ERROR` at the API edge, and with the matching engine's own `TRIGGER_IOC_MUST_NOT_EXPIRE_ERROR` guard when requests reach the engine directly.  That single `expiresAfter` is one absolute deadline shared by both phases — the armed trigger's lifetime AND the fired child's hard on-chain settlement deadline — so an hour spent armed is an hour the child does not get. Both phases stop one **settlement headroom** BEFORE that deadline rather than at it: a fill taken inside the headroom could not be settled on chain before `expiresAfter` and would revert `OrderExpired`, so the exchange closes the window early instead. Concretely, a `GTT` trigger stops being fireable — and a `GTT` child already resting in the book stops being matchable — at `expiresAfter` minus the headroom, and whichever of the two the boundary finds is cancelled there as `CANCELLED` with `cancelReason: GTT_EXPIRED`. Treat the last stretch before `expiresAfter` as dead time and size the deadline for the protective window PLUS the headroom.  The headroom is a venue parameter: the exchange sets it, it is the same for every market, it is neither client-supplied nor published on this API, and it is at least a minute on production deployments. A create whose own `expiresAfter` does not clear it is refused at admission with `ORDER_EXPIRES_TOO_SOON_ERROR` — nothing is armed — rather than accepted into a window it could never fire in.  **Pricing the fired child.** `limitPx` is the worst price the child may execute at, not a pad on `triggerPx`, and admission bounds how far the two may sit apart: `|limitPx − triggerPx| <= triggerPx × band`, per market. The band is a matching-engine setting and is deliberately NOT published — keep `limitPx` close to `triggerPx`. A `limitPx` the band does not admit, and any trigger on a market with no band configured, are both rejected with `TRIGGER_LIMIT_OUTSIDE_BAND_ERROR`, with the `message` distinguishing the two causes.  **Firing.** When the mark price crosses `triggerPx`, the trigger fires into an ordinary order carrying the chosen `timeInForce`, and that order surfaces with `triggered: true`. A fire that commits cancels the OCO sibling — the paired opposite-type stop on the same account and market — with `cancelReason: OCO_SIBLING_FIRED`, including a fire that fills nothing: one fire consumes both legs. A partial `IOC` fire leaves the residual position unprotected. Re-arming is always the user's move; cancelling one leg by hand leaves the sibling armed. A firing stop that would match the account's own resting liquidity sweeps those maker orders out of its way — they are cancelled with `PROTECTIVE_SELF_TRADE_SWEEP` rather than the stop being cancelled, which is what distinguishes the sweep from ordinary `SELF_TRADE_PREVENTION`.  A fire can also be refused before any child exists, and then BOTH legs are cancelled with the reason for the refusal rather than with `OCO_SIBLING_FIRED` — that reason is written only when a fire actually commits. Either there is nothing left to reduce (the position closed or flipped under the stop, or what remains is below one tradeable lot), and both legs are cancelled with `cancelReason: POSITION_CLOSED`; or the pre-trade risk checks refuse the close itself, and both legs are cancelled with `cancelReason: RISK_REJECTED`. Which individual risk check refused it is NOT published — `cancelReasonMessage` is a fixed explanatory string per `cancelReason`, so do not parse it or branch on it. No child order is created on either path, so both cancellations arrive on orders that still carry `triggered: false`. Nothing rests, nothing fills, and the protection is terminally gone — the stop is not retried or re-armed when the account recovers, so re-arming is the user's move.  A resting fired child is an ordinary book order with three exceptions: its fills are clamped to the position still reducible at that moment (a clamp to zero cancels the remainder with `POSITION_CLOSED`), so its displayed remaining quantity can exceed its fillable size; it is **cancel-only**, so a `modifyOrder` against it is refused; and it is **not** removed when the position closes by another route. Firing guarantees the child order, not its execution: the child rests at `limitPx` and fills only if the book comes to it, so a mark that gaps through the level can leave a `GTC` / `GTT` child resting unfilled — and the position unprotected — until it fills, expires, or is cancelled. It blocks the account's own crossing orders through standard self-trade prevention, `POST /v2/cancelAll` clears it while an armed cancel-all-after countdown leaves it in place, and a `GTT` child is removed from the book by the ordinary expiry reaper one settlement headroom BEFORE the shared deadline — not at it — arriving as `CANCELLED` with `cancelReason: GTT_EXPIRED`.  Stops and their fired children are **structurally** reduce-only — they close the position and can never open one — while the signed `reduceOnly` field stays `false`; do not read that `false` as permission to open a position.  Also available on the order-entry WebSocket (`asyncapi-exec-v2.yaml`, `createOrder`). 

        :param create_order_request: (required)
        :type create_order_request: CreateOrderRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._create_order_serialize(
            create_order_request=create_order_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "CreateOrderResponse",
            '400': "RequestError",
            '500': "ServerError",
        }
        response_data = await self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        await response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    async def create_order_with_http_info(
        self,
        create_order_request: CreateOrderRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[CreateOrderResponse]:
        """Create order

        Create a new order (IOC, GTC, GTT, SL, TP). Supports both spot and perp markets.  All order types route through the matching engine on a unified `marketId` namespace; the legacy off-chain conditional-order (DB-backed) path is retired. `timeInForce` is REQUIRED on every create, for every order class — earlier revisions let a trigger create omit it, implying `GTC`; from 3.1.0 an omitted `timeInForce` fails schema validation. Triggers are **perp-only** — a `STOP_LOSS` / `TAKE_PROFIT` on a spot market is rejected.  **Armed phase.** A `STOP_LOSS` / `TAKE_PROFIT` create carries `orderType` + `triggerPx` end-to-end and is **armed and tracked** by the matching engine: the trigger is held in the trigger store rather than the book, so it is not executable liquidity while armed. It stays modifiable and cancellable, and surfaces as `OPEN` on `openOrders`, the `walletOrderChanges` stream, and order history — carrying `triggered: false` on the first two. Order-history rows omit `triggered` entirely: the stored history has no armed-vs-fired discriminator, so its absence there means unknown rather than `false`.  **Choosing the fired child.** `timeInForce` on a trigger create chooses what the stop becomes when it fires: `IOC` (fill what the book offers, cancel the remainder), `GTC` (rest the remainder until cancelled) or `GTT` (rest it until the shared deadline below). A `GTT` trigger MUST carry a future `expiresAfter`; `GTC` and `IOC` triggers MUST omit it — an `IOC` trigger that carries one is rejected with `INPUT_VALIDATION_ERROR` at the API edge, and with the matching engine's own `TRIGGER_IOC_MUST_NOT_EXPIRE_ERROR` guard when requests reach the engine directly.  That single `expiresAfter` is one absolute deadline shared by both phases — the armed trigger's lifetime AND the fired child's hard on-chain settlement deadline — so an hour spent armed is an hour the child does not get. Both phases stop one **settlement headroom** BEFORE that deadline rather than at it: a fill taken inside the headroom could not be settled on chain before `expiresAfter` and would revert `OrderExpired`, so the exchange closes the window early instead. Concretely, a `GTT` trigger stops being fireable — and a `GTT` child already resting in the book stops being matchable — at `expiresAfter` minus the headroom, and whichever of the two the boundary finds is cancelled there as `CANCELLED` with `cancelReason: GTT_EXPIRED`. Treat the last stretch before `expiresAfter` as dead time and size the deadline for the protective window PLUS the headroom.  The headroom is a venue parameter: the exchange sets it, it is the same for every market, it is neither client-supplied nor published on this API, and it is at least a minute on production deployments. A create whose own `expiresAfter` does not clear it is refused at admission with `ORDER_EXPIRES_TOO_SOON_ERROR` — nothing is armed — rather than accepted into a window it could never fire in.  **Pricing the fired child.** `limitPx` is the worst price the child may execute at, not a pad on `triggerPx`, and admission bounds how far the two may sit apart: `|limitPx − triggerPx| <= triggerPx × band`, per market. The band is a matching-engine setting and is deliberately NOT published — keep `limitPx` close to `triggerPx`. A `limitPx` the band does not admit, and any trigger on a market with no band configured, are both rejected with `TRIGGER_LIMIT_OUTSIDE_BAND_ERROR`, with the `message` distinguishing the two causes.  **Firing.** When the mark price crosses `triggerPx`, the trigger fires into an ordinary order carrying the chosen `timeInForce`, and that order surfaces with `triggered: true`. A fire that commits cancels the OCO sibling — the paired opposite-type stop on the same account and market — with `cancelReason: OCO_SIBLING_FIRED`, including a fire that fills nothing: one fire consumes both legs. A partial `IOC` fire leaves the residual position unprotected. Re-arming is always the user's move; cancelling one leg by hand leaves the sibling armed. A firing stop that would match the account's own resting liquidity sweeps those maker orders out of its way — they are cancelled with `PROTECTIVE_SELF_TRADE_SWEEP` rather than the stop being cancelled, which is what distinguishes the sweep from ordinary `SELF_TRADE_PREVENTION`.  A fire can also be refused before any child exists, and then BOTH legs are cancelled with the reason for the refusal rather than with `OCO_SIBLING_FIRED` — that reason is written only when a fire actually commits. Either there is nothing left to reduce (the position closed or flipped under the stop, or what remains is below one tradeable lot), and both legs are cancelled with `cancelReason: POSITION_CLOSED`; or the pre-trade risk checks refuse the close itself, and both legs are cancelled with `cancelReason: RISK_REJECTED`. Which individual risk check refused it is NOT published — `cancelReasonMessage` is a fixed explanatory string per `cancelReason`, so do not parse it or branch on it. No child order is created on either path, so both cancellations arrive on orders that still carry `triggered: false`. Nothing rests, nothing fills, and the protection is terminally gone — the stop is not retried or re-armed when the account recovers, so re-arming is the user's move.  A resting fired child is an ordinary book order with three exceptions: its fills are clamped to the position still reducible at that moment (a clamp to zero cancels the remainder with `POSITION_CLOSED`), so its displayed remaining quantity can exceed its fillable size; it is **cancel-only**, so a `modifyOrder` against it is refused; and it is **not** removed when the position closes by another route. Firing guarantees the child order, not its execution: the child rests at `limitPx` and fills only if the book comes to it, so a mark that gaps through the level can leave a `GTC` / `GTT` child resting unfilled — and the position unprotected — until it fills, expires, or is cancelled. It blocks the account's own crossing orders through standard self-trade prevention, `POST /v2/cancelAll` clears it while an armed cancel-all-after countdown leaves it in place, and a `GTT` child is removed from the book by the ordinary expiry reaper one settlement headroom BEFORE the shared deadline — not at it — arriving as `CANCELLED` with `cancelReason: GTT_EXPIRED`.  Stops and their fired children are **structurally** reduce-only — they close the position and can never open one — while the signed `reduceOnly` field stays `false`; do not read that `false` as permission to open a position.  Also available on the order-entry WebSocket (`asyncapi-exec-v2.yaml`, `createOrder`). 

        :param create_order_request: (required)
        :type create_order_request: CreateOrderRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._create_order_serialize(
            create_order_request=create_order_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "CreateOrderResponse",
            '400': "RequestError",
            '500': "ServerError",
        }
        response_data = await self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        await response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    async def create_order_without_preload_content(
        self,
        create_order_request: CreateOrderRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Create order

        Create a new order (IOC, GTC, GTT, SL, TP). Supports both spot and perp markets.  All order types route through the matching engine on a unified `marketId` namespace; the legacy off-chain conditional-order (DB-backed) path is retired. `timeInForce` is REQUIRED on every create, for every order class — earlier revisions let a trigger create omit it, implying `GTC`; from 3.1.0 an omitted `timeInForce` fails schema validation. Triggers are **perp-only** — a `STOP_LOSS` / `TAKE_PROFIT` on a spot market is rejected.  **Armed phase.** A `STOP_LOSS` / `TAKE_PROFIT` create carries `orderType` + `triggerPx` end-to-end and is **armed and tracked** by the matching engine: the trigger is held in the trigger store rather than the book, so it is not executable liquidity while armed. It stays modifiable and cancellable, and surfaces as `OPEN` on `openOrders`, the `walletOrderChanges` stream, and order history — carrying `triggered: false` on the first two. Order-history rows omit `triggered` entirely: the stored history has no armed-vs-fired discriminator, so its absence there means unknown rather than `false`.  **Choosing the fired child.** `timeInForce` on a trigger create chooses what the stop becomes when it fires: `IOC` (fill what the book offers, cancel the remainder), `GTC` (rest the remainder until cancelled) or `GTT` (rest it until the shared deadline below). A `GTT` trigger MUST carry a future `expiresAfter`; `GTC` and `IOC` triggers MUST omit it — an `IOC` trigger that carries one is rejected with `INPUT_VALIDATION_ERROR` at the API edge, and with the matching engine's own `TRIGGER_IOC_MUST_NOT_EXPIRE_ERROR` guard when requests reach the engine directly.  That single `expiresAfter` is one absolute deadline shared by both phases — the armed trigger's lifetime AND the fired child's hard on-chain settlement deadline — so an hour spent armed is an hour the child does not get. Both phases stop one **settlement headroom** BEFORE that deadline rather than at it: a fill taken inside the headroom could not be settled on chain before `expiresAfter` and would revert `OrderExpired`, so the exchange closes the window early instead. Concretely, a `GTT` trigger stops being fireable — and a `GTT` child already resting in the book stops being matchable — at `expiresAfter` minus the headroom, and whichever of the two the boundary finds is cancelled there as `CANCELLED` with `cancelReason: GTT_EXPIRED`. Treat the last stretch before `expiresAfter` as dead time and size the deadline for the protective window PLUS the headroom.  The headroom is a venue parameter: the exchange sets it, it is the same for every market, it is neither client-supplied nor published on this API, and it is at least a minute on production deployments. A create whose own `expiresAfter` does not clear it is refused at admission with `ORDER_EXPIRES_TOO_SOON_ERROR` — nothing is armed — rather than accepted into a window it could never fire in.  **Pricing the fired child.** `limitPx` is the worst price the child may execute at, not a pad on `triggerPx`, and admission bounds how far the two may sit apart: `|limitPx − triggerPx| <= triggerPx × band`, per market. The band is a matching-engine setting and is deliberately NOT published — keep `limitPx` close to `triggerPx`. A `limitPx` the band does not admit, and any trigger on a market with no band configured, are both rejected with `TRIGGER_LIMIT_OUTSIDE_BAND_ERROR`, with the `message` distinguishing the two causes.  **Firing.** When the mark price crosses `triggerPx`, the trigger fires into an ordinary order carrying the chosen `timeInForce`, and that order surfaces with `triggered: true`. A fire that commits cancels the OCO sibling — the paired opposite-type stop on the same account and market — with `cancelReason: OCO_SIBLING_FIRED`, including a fire that fills nothing: one fire consumes both legs. A partial `IOC` fire leaves the residual position unprotected. Re-arming is always the user's move; cancelling one leg by hand leaves the sibling armed. A firing stop that would match the account's own resting liquidity sweeps those maker orders out of its way — they are cancelled with `PROTECTIVE_SELF_TRADE_SWEEP` rather than the stop being cancelled, which is what distinguishes the sweep from ordinary `SELF_TRADE_PREVENTION`.  A fire can also be refused before any child exists, and then BOTH legs are cancelled with the reason for the refusal rather than with `OCO_SIBLING_FIRED` — that reason is written only when a fire actually commits. Either there is nothing left to reduce (the position closed or flipped under the stop, or what remains is below one tradeable lot), and both legs are cancelled with `cancelReason: POSITION_CLOSED`; or the pre-trade risk checks refuse the close itself, and both legs are cancelled with `cancelReason: RISK_REJECTED`. Which individual risk check refused it is NOT published — `cancelReasonMessage` is a fixed explanatory string per `cancelReason`, so do not parse it or branch on it. No child order is created on either path, so both cancellations arrive on orders that still carry `triggered: false`. Nothing rests, nothing fills, and the protection is terminally gone — the stop is not retried or re-armed when the account recovers, so re-arming is the user's move.  A resting fired child is an ordinary book order with three exceptions: its fills are clamped to the position still reducible at that moment (a clamp to zero cancels the remainder with `POSITION_CLOSED`), so its displayed remaining quantity can exceed its fillable size; it is **cancel-only**, so a `modifyOrder` against it is refused; and it is **not** removed when the position closes by another route. Firing guarantees the child order, not its execution: the child rests at `limitPx` and fills only if the book comes to it, so a mark that gaps through the level can leave a `GTC` / `GTT` child resting unfilled — and the position unprotected — until it fills, expires, or is cancelled. It blocks the account's own crossing orders through standard self-trade prevention, `POST /v2/cancelAll` clears it while an armed cancel-all-after countdown leaves it in place, and a `GTT` child is removed from the book by the ordinary expiry reaper one settlement headroom BEFORE the shared deadline — not at it — arriving as `CANCELLED` with `cancelReason: GTT_EXPIRED`.  Stops and their fired children are **structurally** reduce-only — they close the position and can never open one — while the signed `reduceOnly` field stays `false`; do not read that `false` as permission to open a position.  Also available on the order-entry WebSocket (`asyncapi-exec-v2.yaml`, `createOrder`). 

        :param create_order_request: (required)
        :type create_order_request: CreateOrderRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._create_order_serialize(
            create_order_request=create_order_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "CreateOrderResponse",
            '400': "RequestError",
            '500': "ServerError",
        }
        response_data = await self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _create_order_serialize(
        self,
        create_order_request,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        # process the query parameters
        # process the header parameters
        # process the form parameters
        # process the body parameter
        if create_order_request is not None:
            _body_params = create_order_request


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json'
                ]
            )

        # set the HTTP header `Content-Type`
        if _content_type:
            _header_params['Content-Type'] = _content_type
        else:
            _default_content_type = (
                self.api_client.select_header_content_type(
                    [
                        'application/json'
                    ]
                )
            )
            if _default_content_type is not None:
                _header_params['Content-Type'] = _default_content_type

        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='POST',
            resource_path='/createOrder',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call
    async def modify_order(
        self,
        modify_order_request: ModifyOrderRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ModifyOrderResponse:
        """Modify order

        Modify a resting order in place. The order keeps its `orderId` and `clientOrderId`. Target by `orderId` when present, otherwise by a non-zero `clientOrderId`. JSON Schema validates target presence only; the server enforces the non-zero rule for sole `clientOrderId` targets. The same `clientOrderId` field restates the resting order's immutable client id for signing. If both identifiers are supplied, `orderId` is canonical for lookup and `clientOrderId` is the restated immutable. If the resting order has no client id, omit `clientOrderId`; do not send a placeholder value. **Full restate:** the body carries the same fields as `createOrder` (the complete post-modify `OrderDetails`) plus the target id — restate every value, even unchanged ones; omitted fields never \"inherit from the resting order\". The modifiable fields are class-split: for a `LIMIT` modify they are `limitPx`, `qty`, `postOnly`, `expiresAfter` (and `triggerPx` is forbidden); for a `STOP_LOSS`/`TAKE_PROFIT` modify ONLY `limitPx` and `triggerPx` are modifiable — `qty` stays omitted (wire quantity 0; the signed quantity restates the full-position sentinel), and `postOnly=false`, the `timeInForce` chosen at creation and its `expiresAfter` are restated immutables (changing any returns `INPUT_VALIDATION_ERROR` from the ME's immutable-match): giving a stop a different fired-child time in force, or a different shared expiry, means cancelling it and creating a new one. Only an **armed** trigger (`triggered: false`) is modifiable — once it has fired, the resting child is cancel-only and a modify against it is refused. A re-priced trigger has its post-modify `limitPx`/`triggerPx` pair re-checked against the market's trigger band exactly as at creation, and a pair outside it is rejected with `TRIGGER_LIMIT_OUTSIDE_BAND_ERROR`. The rest (`exchangeId`, `isBuy`, `orderType`, `timeInForce`, `reduceOnly`, `clientOrderId`, `accountId`, `signerWallet`) are immutable and must be restated at the resting order's values — a mismatch is rejected with `INPUT_VALIDATION_ERROR` (the ME's internal `MODIFY_IMMUTABLE_MISMATCH` is surfaced as `INPUT_VALIDATION_ERROR`). A `STOP_LOSS`/`TAKE_PROFIT` order is re-priced in place (full restate): `triggerPx` is REQUIRED on a trigger modify — a trigger modify that omits it is rejected with `INPUT_VALIDATION_ERROR`. `qty` is omitted on a `STOP_LOSS`/`TAKE_PROFIT` modify (the signed quantity restates the full-position sentinel `±int256.max`, sign from `isBuy`) and required for a `LIMIT` modify. A `LIMIT` request whose post-modify state equals the order's current state is rejected with `EMPTY_MODIFY_ERROR`; a trigger restated at unchanged `limitPx` and `triggerPx` is instead accepted and applied, so a stop can be re-signed with a fresh `signature`, `nonce` and `deadline` without moving its prices. Queue priority: a `qty` decrease at an unchanged `limitPx` preserves the order's place in the book; any `limitPx` change or `qty` increase loses it. `timeInForce` is immutable for every class, so `expiresAfter` is omitted for GTC and IOC and set to a future timestamp greater than `deadline` for GTT. A post-only modification that would cross is rejected with no mutation; a non-post-only modification that crosses executes — the response reports `execQty` and `status`, with per-fill detail on the streaming surface, as with `createOrder`. Omit `triggerPx` for a `LIMIT` order. The fresh EIP-712 signature (same `Order` / `OrderDetails` envelope as `createOrder`) is verified over exactly the restated fields; a fresh `nonce` is required. Also available on the order-entry WebSocket (`asyncapi-exec-v2.yaml`, `modifyOrder`). 

        :param modify_order_request: (required)
        :type modify_order_request: ModifyOrderRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._modify_order_serialize(
            modify_order_request=modify_order_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "ModifyOrderResponse",
            '400': "RequestError",
            '500': "ServerError",
        }
        response_data = await self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        await response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    async def modify_order_with_http_info(
        self,
        modify_order_request: ModifyOrderRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[ModifyOrderResponse]:
        """Modify order

        Modify a resting order in place. The order keeps its `orderId` and `clientOrderId`. Target by `orderId` when present, otherwise by a non-zero `clientOrderId`. JSON Schema validates target presence only; the server enforces the non-zero rule for sole `clientOrderId` targets. The same `clientOrderId` field restates the resting order's immutable client id for signing. If both identifiers are supplied, `orderId` is canonical for lookup and `clientOrderId` is the restated immutable. If the resting order has no client id, omit `clientOrderId`; do not send a placeholder value. **Full restate:** the body carries the same fields as `createOrder` (the complete post-modify `OrderDetails`) plus the target id — restate every value, even unchanged ones; omitted fields never \"inherit from the resting order\". The modifiable fields are class-split: for a `LIMIT` modify they are `limitPx`, `qty`, `postOnly`, `expiresAfter` (and `triggerPx` is forbidden); for a `STOP_LOSS`/`TAKE_PROFIT` modify ONLY `limitPx` and `triggerPx` are modifiable — `qty` stays omitted (wire quantity 0; the signed quantity restates the full-position sentinel), and `postOnly=false`, the `timeInForce` chosen at creation and its `expiresAfter` are restated immutables (changing any returns `INPUT_VALIDATION_ERROR` from the ME's immutable-match): giving a stop a different fired-child time in force, or a different shared expiry, means cancelling it and creating a new one. Only an **armed** trigger (`triggered: false`) is modifiable — once it has fired, the resting child is cancel-only and a modify against it is refused. A re-priced trigger has its post-modify `limitPx`/`triggerPx` pair re-checked against the market's trigger band exactly as at creation, and a pair outside it is rejected with `TRIGGER_LIMIT_OUTSIDE_BAND_ERROR`. The rest (`exchangeId`, `isBuy`, `orderType`, `timeInForce`, `reduceOnly`, `clientOrderId`, `accountId`, `signerWallet`) are immutable and must be restated at the resting order's values — a mismatch is rejected with `INPUT_VALIDATION_ERROR` (the ME's internal `MODIFY_IMMUTABLE_MISMATCH` is surfaced as `INPUT_VALIDATION_ERROR`). A `STOP_LOSS`/`TAKE_PROFIT` order is re-priced in place (full restate): `triggerPx` is REQUIRED on a trigger modify — a trigger modify that omits it is rejected with `INPUT_VALIDATION_ERROR`. `qty` is omitted on a `STOP_LOSS`/`TAKE_PROFIT` modify (the signed quantity restates the full-position sentinel `±int256.max`, sign from `isBuy`) and required for a `LIMIT` modify. A `LIMIT` request whose post-modify state equals the order's current state is rejected with `EMPTY_MODIFY_ERROR`; a trigger restated at unchanged `limitPx` and `triggerPx` is instead accepted and applied, so a stop can be re-signed with a fresh `signature`, `nonce` and `deadline` without moving its prices. Queue priority: a `qty` decrease at an unchanged `limitPx` preserves the order's place in the book; any `limitPx` change or `qty` increase loses it. `timeInForce` is immutable for every class, so `expiresAfter` is omitted for GTC and IOC and set to a future timestamp greater than `deadline` for GTT. A post-only modification that would cross is rejected with no mutation; a non-post-only modification that crosses executes — the response reports `execQty` and `status`, with per-fill detail on the streaming surface, as with `createOrder`. Omit `triggerPx` for a `LIMIT` order. The fresh EIP-712 signature (same `Order` / `OrderDetails` envelope as `createOrder`) is verified over exactly the restated fields; a fresh `nonce` is required. Also available on the order-entry WebSocket (`asyncapi-exec-v2.yaml`, `modifyOrder`). 

        :param modify_order_request: (required)
        :type modify_order_request: ModifyOrderRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._modify_order_serialize(
            modify_order_request=modify_order_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "ModifyOrderResponse",
            '400': "RequestError",
            '500': "ServerError",
        }
        response_data = await self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        await response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    async def modify_order_without_preload_content(
        self,
        modify_order_request: ModifyOrderRequest,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Modify order

        Modify a resting order in place. The order keeps its `orderId` and `clientOrderId`. Target by `orderId` when present, otherwise by a non-zero `clientOrderId`. JSON Schema validates target presence only; the server enforces the non-zero rule for sole `clientOrderId` targets. The same `clientOrderId` field restates the resting order's immutable client id for signing. If both identifiers are supplied, `orderId` is canonical for lookup and `clientOrderId` is the restated immutable. If the resting order has no client id, omit `clientOrderId`; do not send a placeholder value. **Full restate:** the body carries the same fields as `createOrder` (the complete post-modify `OrderDetails`) plus the target id — restate every value, even unchanged ones; omitted fields never \"inherit from the resting order\". The modifiable fields are class-split: for a `LIMIT` modify they are `limitPx`, `qty`, `postOnly`, `expiresAfter` (and `triggerPx` is forbidden); for a `STOP_LOSS`/`TAKE_PROFIT` modify ONLY `limitPx` and `triggerPx` are modifiable — `qty` stays omitted (wire quantity 0; the signed quantity restates the full-position sentinel), and `postOnly=false`, the `timeInForce` chosen at creation and its `expiresAfter` are restated immutables (changing any returns `INPUT_VALIDATION_ERROR` from the ME's immutable-match): giving a stop a different fired-child time in force, or a different shared expiry, means cancelling it and creating a new one. Only an **armed** trigger (`triggered: false`) is modifiable — once it has fired, the resting child is cancel-only and a modify against it is refused. A re-priced trigger has its post-modify `limitPx`/`triggerPx` pair re-checked against the market's trigger band exactly as at creation, and a pair outside it is rejected with `TRIGGER_LIMIT_OUTSIDE_BAND_ERROR`. The rest (`exchangeId`, `isBuy`, `orderType`, `timeInForce`, `reduceOnly`, `clientOrderId`, `accountId`, `signerWallet`) are immutable and must be restated at the resting order's values — a mismatch is rejected with `INPUT_VALIDATION_ERROR` (the ME's internal `MODIFY_IMMUTABLE_MISMATCH` is surfaced as `INPUT_VALIDATION_ERROR`). A `STOP_LOSS`/`TAKE_PROFIT` order is re-priced in place (full restate): `triggerPx` is REQUIRED on a trigger modify — a trigger modify that omits it is rejected with `INPUT_VALIDATION_ERROR`. `qty` is omitted on a `STOP_LOSS`/`TAKE_PROFIT` modify (the signed quantity restates the full-position sentinel `±int256.max`, sign from `isBuy`) and required for a `LIMIT` modify. A `LIMIT` request whose post-modify state equals the order's current state is rejected with `EMPTY_MODIFY_ERROR`; a trigger restated at unchanged `limitPx` and `triggerPx` is instead accepted and applied, so a stop can be re-signed with a fresh `signature`, `nonce` and `deadline` without moving its prices. Queue priority: a `qty` decrease at an unchanged `limitPx` preserves the order's place in the book; any `limitPx` change or `qty` increase loses it. `timeInForce` is immutable for every class, so `expiresAfter` is omitted for GTC and IOC and set to a future timestamp greater than `deadline` for GTT. A post-only modification that would cross is rejected with no mutation; a non-post-only modification that crosses executes — the response reports `execQty` and `status`, with per-fill detail on the streaming surface, as with `createOrder`. Omit `triggerPx` for a `LIMIT` order. The fresh EIP-712 signature (same `Order` / `OrderDetails` envelope as `createOrder`) is verified over exactly the restated fields; a fresh `nonce` is required. Also available on the order-entry WebSocket (`asyncapi-exec-v2.yaml`, `modifyOrder`). 

        :param modify_order_request: (required)
        :type modify_order_request: ModifyOrderRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._modify_order_serialize(
            modify_order_request=modify_order_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "ModifyOrderResponse",
            '400': "RequestError",
            '500': "ServerError",
        }
        response_data = await self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _modify_order_serialize(
        self,
        modify_order_request,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        # process the query parameters
        # process the header parameters
        # process the form parameters
        # process the body parameter
        if modify_order_request is not None:
            _body_params = modify_order_request


        # set the HTTP header `Accept`
        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json'
                ]
            )

        # set the HTTP header `Content-Type`
        if _content_type:
            _header_params['Content-Type'] = _content_type
        else:
            _default_content_type = (
                self.api_client.select_header_content_type(
                    [
                        'application/json'
                    ]
                )
            )
            if _default_content_type is not None:
                _header_params['Content-Type'] = _default_content_type

        # authentication setting
        _auth_settings: List[str] = [
        ]

        return self.api_client.param_serialize(
            method='POST',
            resource_path='/modifyOrder',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )



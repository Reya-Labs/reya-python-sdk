# coding: utf-8

"""
    Reya DEX Trading API v2

    No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

    The version of the OpenAPI document: 3.0.5
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class ModifyOrderRequest(BaseModel):
    """
    Modifies a resting order in place. The order keeps its `orderId` and `clientOrderId` in all cases. Target exactly one of `orderId` / `clientOrderId`; supplying both or neither is rejected with `INPUT_VALIDATION_ERROR`, and a target that does not resolve to a live resting order is rejected with `ORDER_NOT_FOUND`. The four modifiable fields — `limitPx`, `qty`, `postOnly`, `expiresAfter` — are all required and carry the complete post-modify state of the order; there is no omitted-means-inherited shorthand. The request's EIP-712 signature covers the full post-modify order state, so the client necessarily knows every value; requiring all four keeps the wire contract identical to the signed contract and eliminates sentinel/inherit ambiguity. A request whose post-modify state is identical to the order's current state is rejected with `EMPTY_MODIFY_ERROR`. Non-modifiable attributes (`side`, `timeInForce`, `reduceOnly`, `accountId`, `signerWallet`, `clientOrderId`) always carry over from the resting order. Queue priority: a `qty` decrease at an unchanged `limitPx` preserves the order's place in the book; any `limitPx` change or `qty` increase loses it (re-queued at the new level). `qty` is the TOTAL order quantity, not the remaining quantity, and must be strictly greater than the order's `cumQty` (else `MODIFY_QTY_BELOW_FILLED_ERROR`). If the post-modify order is post-only and would cross, the modification is rejected with `POST_ONLY_WOULD_CROSS_ERROR` and the resting order is left untouched, priority intact. If it is not post-only and the new `limitPx` crosses, the modification executes immediately — the response reports the executed quantity (`execQty`) and resulting `status`, with per-fill detail delivered on the wallet executions and `walletOrderChanges` streams, exactly as for `createOrder`. Requires a fresh EIP-712 signature over the full post-modify order state (the same `Order` envelope as `createOrder`, with the modified values substituted into `OrderDetails` — the signature covers exactly the four values sent plus the inherited immutables) and a fresh `nonce`. See `docs/eip712.md` for the signing algorithm and exact typehash strings.
    """ # noqa: E501
    order_id: Optional[StrictStr] = Field(default=None, description="Internal matching engine order ID of the order to modify. Exactly one of `orderId` or `clientOrderId` must be provided.", alias="orderId")
    client_order_id: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=None, alias="clientOrderId")
    symbol: Annotated[str, Field(strict=True)] = Field(description="Trading symbol (e.g., BTCRUSDPERP, WETHRUSD)")
    account_id: Annotated[int, Field(strict=True, ge=0)] = Field(alias="accountId")
    limit_px: Annotated[str, Field(strict=True)] = Field(alias="limitPx")
    qty: Annotated[str, Field(strict=True)]
    post_only: StrictBool = Field(description="The post-modify post-only (maker-only) flag. Always required — send the complete intended value even when it is unchanged from the resting order. If true and the post-modify order would cross, the modification is rejected with `POST_ONLY_WOULD_CROSS_ERROR` and the resting order is unchanged.", alias="postOnly")
    expires_after: Annotated[int, Field(strict=True, ge=0)] = Field(alias="expiresAfter")
    signature: StrictStr = Field(description="Fresh EIP-712 signature over the full post-modify order state — the same `Order` envelope as `createOrder`, with the modified values substituted into `OrderDetails`. See `docs/eip712.md` for the exact typehash string and signing algorithm.")
    nonce: StrictStr = Field(description="Monotonically increasing per-signer nonce. A fresh nonce is required for every modification; replayed nonces are rejected with `INVALID_NONCE_ERROR`.")
    signer_wallet: Annotated[str, Field(strict=True)] = Field(alias="signerWallet")
    deadline: Annotated[int, Field(strict=True, ge=0)]
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["orderId", "clientOrderId", "symbol", "accountId", "limitPx", "qty", "postOnly", "expiresAfter", "signature", "nonce", "signerWallet", "deadline"]

    @field_validator('symbol')
    def symbol_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^[A-Za-z0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[A-Za-z0-9]+$/")
        return value

    @field_validator('limit_px')
    def limit_px_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^-?\d+(\.\d+)?([eE][+-]?\d+)?$", value):
            raise ValueError(r"must validate the regular expression /^-?\d+(\.\d+)?([eE][+-]?\d+)?$/")
        return value

    @field_validator('qty')
    def qty_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^\d+(\.\d+)?([eE][+-]?\d+)?$", value):
            raise ValueError(r"must validate the regular expression /^\d+(\.\d+)?([eE][+-]?\d+)?$/")
        return value

    @field_validator('signer_wallet')
    def signer_wallet_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^0x[a-fA-F0-9]{40}$", value):
            raise ValueError(r"must validate the regular expression /^0x[a-fA-F0-9]{40}$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ModifyOrderRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ModifyOrderRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "orderId": obj.get("orderId"),
            "clientOrderId": obj.get("clientOrderId"),
            "symbol": obj.get("symbol"),
            "accountId": obj.get("accountId"),
            "limitPx": obj.get("limitPx"),
            "qty": obj.get("qty"),
            "postOnly": obj.get("postOnly"),
            "expiresAfter": obj.get("expiresAfter"),
            "signature": obj.get("signature"),
            "nonce": obj.get("nonce"),
            "signerWallet": obj.get("signerWallet"),
            "deadline": obj.get("deadline")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj



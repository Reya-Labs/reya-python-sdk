# coding: utf-8

"""
    Reya DEX Trading API v2

    No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

    The version of the OpenAPI document: 3.0.10
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from sdk.open_api.models.order_type import OrderType
from sdk.open_api.models.time_in_force import TimeInForce
from typing import Optional, Set
from typing_extensions import Self

class ModifyOrderRequest(BaseModel):
    """
    Modifies a resting order in place. The order keeps its `orderId` and `clientOrderId`. Target exactly one of `orderId` / `clientOrderId`; supplying both or neither is rejected with `INPUT_VALIDATION_ERROR`, and a target that does not resolve to a live resting order is rejected with `ORDER_NOT_FOUND`. **Full restate:** the request carries the SAME fields as `createOrder` (the complete post-modify `OrderDetails`) plus the target id — restate every value at its post-modify state, even unchanged ones; there is no omitted-means-inherited shorthand. The five modifiable fields are `limitPx`, `qty`, `postOnly`, `expiresAfter`, and `triggerPx` — for a `STOP_LOSS` / `TAKE_PROFIT` order the trigger price can be re-priced in place (no cancel+recreate). The remaining signed `OrderDetails` fields (`exchangeId`, `isBuy`/side, `orderType`, `timeInForce`, `reduceOnly`, `clientOrderId`, `accountId`, `signerWallet`) are IMMUTABLE: restate each at the resting order's value — the matching engine verifies each equals the resting order and rejects a mismatch with `INPUT_VALIDATION_ERROR` (the matching engine's internal `MODIFY_IMMUTABLE_MISMATCH` is surfaced to clients as `INPUT_VALIDATION_ERROR`; the human-readable `message` names the offending field). `orderType` is immutable — a modify re-prices a trigger but never converts an order between `LIMIT` and `STOP_LOSS` / `TAKE_PROFIT`. **Availability:** editing a `STOP_LOSS` / `TAKE_PROFIT` order (including its `triggerPx`) depends on native SL/TP trigger orders landing in the matching engine; until then, modify accepts only `LIMIT` orders and a `STOP_LOSS` / `TAKE_PROFIT` modify is rejected with `MODIFY_ORDER_OTHER_ERROR`. A `LIMIT` order's `triggerPx` is `0`. The EIP-712 signature is verified over exactly the restated fields (the same `Order` / `OrderDetails` envelope as `createOrder`), so it needs no resting-order lookup; a fresh `nonce` is required. A request whose post-modify state is identical to the order's current state is rejected with `EMPTY_MODIFY_ERROR`. Queue priority: a `qty` decrease at an unchanged `limitPx` preserves the order's place in the book; any `limitPx` change or `qty` increase loses it (re-queued at the new level). `qty` is the TOTAL order quantity, not remaining, and must be strictly greater than the order's `cumQty` (else `MODIFY_QTY_BELOW_FILLED_ERROR`). `timeInForce` is immutable — a modify cannot flip GTC↔GTT; the restated `timeInForce` must match the resting order and `expiresAfter` must stay consistent with it (`0` for GTC, a non-zero future timestamp for GTT). If the post-modify order is post-only and would cross, the modification is rejected with `POST_ONLY_WOULD_CROSS_ERROR` and the resting order is left untouched, priority intact. If it is not post-only and the new `limitPx` crosses, the modification executes immediately — the response reports the executed quantity (`execQty`) and resulting `status`, with per-fill detail delivered on the wallet executions and `walletOrderChanges` streams, exactly as for `createOrder`. See `docs/eip712.md` for the signing algorithm and exact typehash strings.
    """ # noqa: E501
    order_id: Optional[StrictStr] = Field(default=None, description="Internal matching engine order ID of the order to modify. Exactly one of `orderId` or `clientOrderId` must be provided.", alias="orderId")
    client_order_id: Optional[StrictStr] = Field(default=None, description="Client-provided order ID of the order to modify (the same `clientOrderId` supplied in CreateOrderRequest), as a decimal string (`uint64`). Exactly one of `orderId` or `clientOrderId` must be provided. The value `\"0\"` is the \"no tag\" sentinel and is NOT a valid sole target — an order created without a client order id can only be modified by `orderId`. Supplying both `orderId` and `clientOrderId`, or neither, is rejected with `INPUT_VALIDATION_ERROR`. The modification cannot assign a new `clientOrderId`; the resting order's value is preserved.", alias="clientOrderId")
    symbol: Annotated[str, Field(strict=True)] = Field(description="Trading symbol (e.g., BTCRUSDPERP, WETHRUSD)")
    account_id: Annotated[int, Field(strict=True, ge=0)] = Field(alias="accountId")
    exchange_id: Annotated[int, Field(strict=True, ge=0)] = Field(alias="exchangeId")
    is_buy: StrictBool = Field(description="Order side. Immutable — restate the resting order's value. Combined with `qty`, sets the signed `OrderDetails.quantity` (int256). A mismatch is rejected with `INPUT_VALIDATION_ERROR`.", alias="isBuy")
    order_type: OrderType = Field(alias="orderType")
    time_in_force: TimeInForce = Field(alias="timeInForce")
    trigger_px: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, alias="triggerPx")
    reduce_only: StrictBool = Field(description="On-chain `OrderDetails.reduceOnly`. Immutable — restate the resting order's value. A mismatch is rejected with `INPUT_VALIDATION_ERROR`.", alias="reduceOnly")
    limit_px: Annotated[str, Field(strict=True)] = Field(alias="limitPx")
    qty: Annotated[str, Field(strict=True)]
    post_only: StrictBool = Field(description="The post-modify post-only (maker-only) flag. Always required — send the complete intended value even when it is unchanged from the resting order. If true and the post-modify order would cross, the modification is rejected with `POST_ONLY_WOULD_CROSS_ERROR` and the resting order is unchanged.", alias="postOnly")
    expires_after: Annotated[int, Field(strict=True, ge=0)] = Field(alias="expiresAfter")
    signature: StrictStr = Field(description="Fresh EIP-712 signature over the full post-modify order state — the same `Order` envelope as `createOrder`, with the modified values substituted into `OrderDetails`. See `docs/eip712.md` for the exact typehash string and signing algorithm.")
    nonce: StrictStr = Field(description="Monotonically increasing per-signer nonce. A fresh nonce is required for every modification; replayed nonces are rejected with `INVALID_NONCE_ERROR`.")
    signer_wallet: Annotated[str, Field(strict=True)] = Field(alias="signerWallet")
    deadline: Annotated[int, Field(strict=True, ge=0)]
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["orderId", "clientOrderId", "symbol", "accountId", "exchangeId", "isBuy", "orderType", "timeInForce", "triggerPx", "reduceOnly", "limitPx", "qty", "postOnly", "expiresAfter", "signature", "nonce", "signerWallet", "deadline"]

    @field_validator('symbol')
    def symbol_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^[A-Za-z0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[A-Za-z0-9]+$/")
        return value

    @field_validator('trigger_px')
    def trigger_px_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^-?\d+(\.\d+)?([eE][+-]?\d+)?$", value):
            raise ValueError(r"must validate the regular expression /^-?\d+(\.\d+)?([eE][+-]?\d+)?$/")
        return value

    @field_validator('limit_px')
    def limit_px_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^-?\d+(\.\d+)?([eE][+-]?\d+)?$", value):
            raise ValueError(r"must validate the regular expression /^-?\d+(\.\d+)?([eE][+-]?\d+)?$/")
        return value

    @field_validator('qty')
    def qty_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^\d+(\.\d+)?([eE][+-]?\d+)?$", value):
            raise ValueError(r"must validate the regular expression /^\d+(\.\d+)?([eE][+-]?\d+)?$/")
        return value

    @field_validator('signer_wallet')
    def signer_wallet_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^0x[a-fA-F0-9]{40}$", value):
            raise ValueError(r"must validate the regular expression /^0x[a-fA-F0-9]{40}$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ModifyOrderRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ModifyOrderRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "orderId": obj.get("orderId"),
            "clientOrderId": obj.get("clientOrderId"),
            "symbol": obj.get("symbol"),
            "accountId": obj.get("accountId"),
            "exchangeId": obj.get("exchangeId"),
            "isBuy": obj.get("isBuy"),
            "orderType": obj.get("orderType"),
            "timeInForce": obj.get("timeInForce"),
            "triggerPx": obj.get("triggerPx"),
            "reduceOnly": obj.get("reduceOnly"),
            "limitPx": obj.get("limitPx"),
            "qty": obj.get("qty"),
            "postOnly": obj.get("postOnly"),
            "expiresAfter": obj.get("expiresAfter"),
            "signature": obj.get("signature"),
            "nonce": obj.get("nonce"),
            "signerWallet": obj.get("signerWallet"),
            "deadline": obj.get("deadline")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj



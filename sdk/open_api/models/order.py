# coding: utf-8

"""
    Reya DEX Trading API v2

    No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

    The version of the OpenAPI document: 3.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from sdk.open_api.models.cancel_reason import CancelReason
from sdk.open_api.models.order_status import OrderStatus
from sdk.open_api.models.order_type import OrderType
from sdk.open_api.models.side import Side
from sdk.open_api.models.time_in_force import TimeInForce
from typing import Optional, Set
from typing_extensions import Self

class Order(BaseModel):
    """
    Represents a public order state. Emitted as openOrders snapshots, orderChanges WS events, and orderHistory REST rows. For event-style emissions (orderChanges WS, orderHistory REST), `lastUpdateAt` reflects the matching-engine event time and a single order may produce multiple rows as it moves through the public `OPEN`, `FILLED`, and `CANCELLED` statuses. Matching-engine partial-fill states surface as `OPEN`; request-level rejects are returned as errors rather than order-status rows. Event-style emissions include `sequenceNumber`; resting snapshots omit it.
    """ # noqa: E501
    exchange_id: Annotated[int, Field(strict=True, ge=0)] = Field(alias="exchangeId")
    symbol: Annotated[str, Field(strict=True)] = Field(description="Trading symbol (e.g., BTCRUSDPERP, WETHRUSD)")
    account_id: Annotated[int, Field(strict=True, ge=0)] = Field(alias="accountId")
    order_id: StrictStr = Field(alias="orderId")
    sequence_number: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=None, alias="sequenceNumber")
    client_order_id: Optional[StrictStr] = Field(default=None, description="Client-provided order ID, as a decimal string (`uint64`). Present when the order has a non-zero client id; omitted otherwise.", alias="clientOrderId")
    qty: Optional[Annotated[str, Field(strict=True)]] = None
    exec_qty: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, alias="execQty")
    cum_qty: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, alias="cumQty")
    first_fill_id: Optional[StrictStr] = Field(default=None, description="Matching-engine fill nonce of the first fill this update represents. Together with fillCount it identifies the fills as a contiguous nonce range [firstFillId, firstFillId + fillCount - 1]. For a taker update, the first fill of its matching round; for a maker update, its single fill. Present only on fill updates; absent for non-fill updates and resting-order snapshots.", alias="firstFillId")
    fill_count: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=None, alias="fillCount")
    side: Side
    limit_px: Annotated[str, Field(strict=True)] = Field(alias="limitPx")
    order_type: OrderType = Field(alias="orderType")
    trigger_px: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, alias="triggerPx")
    triggered: Optional[StrictBool] = Field(default=None, description="Armed-vs-fired discriminator for protective stops. `false` while a `STOP_LOSS` / `TAKE_PROFIT` is armed in the matching engine's trigger store: it holds no place in the book, is not executable liquidity, and stays modifiable and cancellable. `true` once it has fired and the resulting child order is resting in the book under the `timeInForce` the create chose. Use it to know that a `modifyOrder` will be refused — a fired child is cancel-only. A fired child's remaining quantity is an upper bound rather than a fillable size: every fill is clamped to the position still reducible at that instant, a clamp to zero cancels the remainder with `cancelReason: POSITION_CLOSED`, and the child is not removed when the position closes by some other route. It blocks the account's own crossing orders through standard self-trade prevention, `POST /v2/cancelAll` cancels it like any other resting order, and an armed cancel-all-after countdown leaves it in place exactly as it leaves an armed trigger. Never `true` on a `LIMIT` order. Read absence by surface. It is omitted on `orderHistory` REST rows, always: the stored history carries no armed-vs-fired discriminator, so the field is left out rather than asserted, and its absence there means UNKNOWN — do not read it as `false`, and use `openOrders` or the `walletOrderChanges` stream when you need the discriminator. On those two surfaces it is present whenever the deployment supports trigger firing, and an omitted value means a deployment that predates it and so may be treated as `false`.")
    time_in_force: Optional[TimeInForce] = Field(default=None, alias="timeInForce")
    expires_after: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=None, alias="expiresAfter")
    reduce_only: Optional[StrictBool] = Field(default=None, description="Whether this is a reduce-only order, exclusively used for LIMIT IOC orders. `STOP_LOSS` / `TAKE_PROFIT` orders and the children they fire into are reduce-only by construction — they close the position and can never open one — yet still report `false` here, because this field mirrors the signed on-chain `OrderDetails.reduceOnly`. Do not read `false` on a stop as permission to open a position.", alias="reduceOnly")
    post_only: Optional[StrictBool] = Field(default=None, description="Whether this is a post-only (maker-only) order. Mirrors `CreateOrderRequest.postOnly`; updated by `modifyOrder`.", alias="postOnly")
    status: OrderStatus
    created_at: Annotated[int, Field(strict=True, ge=0)] = Field(alias="createdAt")
    last_update_at: Annotated[int, Field(strict=True, ge=0)] = Field(alias="lastUpdateAt")
    cancel_reason: Optional[CancelReason] = Field(default=None, alias="cancelReason")
    cancel_reason_message: Optional[StrictStr] = Field(default=None, description="Human-readable explanation of `cancelReason`. Present only when `cancelReason` is present.", alias="cancelReasonMessage")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["exchangeId", "symbol", "accountId", "orderId", "sequenceNumber", "clientOrderId", "qty", "execQty", "cumQty", "firstFillId", "fillCount", "side", "limitPx", "orderType", "triggerPx", "triggered", "timeInForce", "expiresAfter", "reduceOnly", "postOnly", "status", "createdAt", "lastUpdateAt", "cancelReason", "cancelReasonMessage"]

    @field_validator('symbol')
    def symbol_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^[A-Za-z0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[A-Za-z0-9]+$/")
        return value

    @field_validator('qty')
    def qty_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^\d+(\.\d+)?([eE][+-]?\d+)?$", value):
            raise ValueError(r"must validate the regular expression /^\d+(\.\d+)?([eE][+-]?\d+)?$/")
        return value

    @field_validator('exec_qty')
    def exec_qty_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^\d+(\.\d+)?([eE][+-]?\d+)?$", value):
            raise ValueError(r"must validate the regular expression /^\d+(\.\d+)?([eE][+-]?\d+)?$/")
        return value

    @field_validator('cum_qty')
    def cum_qty_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^\d+(\.\d+)?([eE][+-]?\d+)?$", value):
            raise ValueError(r"must validate the regular expression /^\d+(\.\d+)?([eE][+-]?\d+)?$/")
        return value

    @field_validator('limit_px')
    def limit_px_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^-?\d+(\.\d+)?([eE][+-]?\d+)?$", value):
            raise ValueError(r"must validate the regular expression /^-?\d+(\.\d+)?([eE][+-]?\d+)?$/")
        return value

    @field_validator('trigger_px')
    def trigger_px_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^-?\d+(\.\d+)?([eE][+-]?\d+)?$", value):
            raise ValueError(r"must validate the regular expression /^-?\d+(\.\d+)?([eE][+-]?\d+)?$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Order from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Order from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "exchangeId": obj.get("exchangeId"),
            "symbol": obj.get("symbol"),
            "accountId": obj.get("accountId"),
            "orderId": obj.get("orderId"),
            "sequenceNumber": obj.get("sequenceNumber"),
            "clientOrderId": obj.get("clientOrderId"),
            "qty": obj.get("qty"),
            "execQty": obj.get("execQty"),
            "cumQty": obj.get("cumQty"),
            "firstFillId": obj.get("firstFillId"),
            "fillCount": obj.get("fillCount"),
            "side": obj.get("side"),
            "limitPx": obj.get("limitPx"),
            "orderType": obj.get("orderType"),
            "triggerPx": obj.get("triggerPx"),
            "triggered": obj.get("triggered"),
            "timeInForce": obj.get("timeInForce"),
            "expiresAfter": obj.get("expiresAfter"),
            "reduceOnly": obj.get("reduceOnly"),
            "postOnly": obj.get("postOnly"),
            "status": obj.get("status"),
            "createdAt": obj.get("createdAt"),
            "lastUpdateAt": obj.get("lastUpdateAt"),
            "cancelReason": obj.get("cancelReason"),
            "cancelReasonMessage": obj.get("cancelReasonMessage")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


